# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
.next/
out/
build/
dist/

# Test coverage
coverage/

# Generated agent artifacts — produced by scripts/sync-agents.mjs. Prettier owns
# the authored sources (.agents/skills/**, .claude/agents/**); the generator owns
# these mirrors, so formatting them would just fight the next regeneration.
.claude/skills/
.codex/

# Shared verbatim across repositories — copied byte-for-byte from the canonical source, so the
# local Prettier config must not reformat them.
scripts/sync-agents.mjs
scripts/sync-agents.test.mjs

# Agent scratch — untracked working files, not project source. Prettier does not read
# nested .gitignore files, so without this /ship's format:check fails on them.
.superpowers/

# Misc
.DS_Store
*.pem

# Logs
*.log

# Lock files
package-lock.json
yarn.lock
pnpm-lock.yaml
