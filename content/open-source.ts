/**
 * Open Source copy. The repo list itself is fetched at build time by
 * `lib/github.ts` — only the surrounding prose lives here.
 */
export const openSource = {
  subtitle: "Personal projects I build and maintain in the open — pulled live from GitHub.",
} as const;
